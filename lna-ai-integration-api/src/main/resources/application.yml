server:
  port: 8080
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
        api-key: ${OPENAI_API_KEY}

      chat:
        options:
          model: "o1"
openai:
  api:
    key: ${OPENAI_API_KEY}